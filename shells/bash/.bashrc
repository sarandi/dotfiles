
#20190815 - commented this out; this is all loaded by apps/.apps
#export NVM_DIR="$HOME/.nvm"
#[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
#[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion

# again, all of these are already present in shells/bash/.path
#export PATH="$PATH:$HOME/.rvm/bin:/usr/local/Cellar/qt5/5.5.0/bin/:/usr/local/bin:$NVM_DIR" # Add RVM to PATH for scripting


